# Pandas Basic Practice
Exploring fundamental pandas operations using small numpy arrays
** Data Structures
      1. Creating arrays
      2. Building Dataframes from Dictionaries
      3. Clean printing using .to_string()
** Filtering and Conditions
      1. Boolean indexing
      2. And , Or filters
      3. Conditional column creation
      4. .apply() custom function
** Statistics
      1. .mean(), .median(), .mode(), .min(), .max()
      2. .idxmax(), .idxmin()
      3. .value_counts()
      4. .map()
** Sorting and Ranking
      1. .sort_values()
      2. .head(), .tail()
      3. .rank()
      4. .transform()
      5. .groupby()
